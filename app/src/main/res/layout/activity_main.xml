<?xml version="1.0" encoding="utf-8"?>
<!--
    activity_main.xml
    Host for 3 fragments via NavHostFragment.
    Structure: CoordinatorLayout root
      ├── NavHostFragment (fills all space above nav)
      └── BottomNavigationView (HUD bar, pinned bottom, 64dp height)
    CRT scanline overlay is applied as a foreground drawable on root.
-->
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/coordinator_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    tools:context=".MainActivity">

    <!-- NavHostFragment: all fragments render here -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="64dp"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <!-- Bottom Navigation: 8-bit HUD bar -->
    <!-- Border-top = 4dp double outline, no elevation, sharp corners -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        style="@style/Widget.BitStreamNews.BottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_gravity="bottom"
        android:background="?attr/colorNavBackground"
        android:paddingTop="0dp"
        android:paddingBottom="0dp"
        app:itemIconSize="20dp"
        app:labelVisibilityMode="labeled"
        app:layout_behavior="@string/hide_bottom_view_on_scroll_behavior"
        app:menu="@menu/bottom_nav_menu" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>